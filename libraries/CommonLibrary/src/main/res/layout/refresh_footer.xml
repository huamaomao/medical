<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/refresh_bg"
    android:gravity="center"
    android:padding="@dimen/refresh_padding"
    android:visibility="gone">

    <ProgressBar android:id="@+id/pb_loading"
        style="@android:style/Widget.DeviceDefault.Light.ProgressBar.Inverse"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:indeterminate="true" />
    <TextView android:id="@+id/tv_more"
        android:layout_toRightOf="@id/pb_loading"
        android:layout_centerInParent="true"
        android:text="@string/loading"
        android:textSize="@dimen/refresh_loading_size"
        android:textColor="@color/refresh_msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
</RelativeLayout>